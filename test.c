#include <stdio.h>
#include <stdlib.h>
#include "Date.h"
#include "NewCart.h"


int test()
{
	/*
	// ** TEST NUMBER 1 : DATE --> PASSED **
	int input;
	Date date;


	// Get the integer input from the user
	printf("Enter a date in format DDMMYYYY: ");
	scanf_s("%d", &input);
	newDate(&date, input);
	printf_s("Day: %02d\n", date.day);
	printf_s("Month: %02d\n", date.month);
	printf_s("Year: %04d\n", date.year);
	// ** END TEST NUMBER 1 : DATE --> PASSED **


	// ** TEST NUMBER 2 : Shopping cart --> PASSED **
	ShoppingCart cart;
	initializeCart(&cart);

	// Create a new product
	Product x;
	newProduct(&x);

	// Create a ShoppingItem for the product
	ShoppingItem xi = { x, 2 }; // Adding 2 units of the product

	// Add the item to the cart
	addItemToCart(&cart, &xi);

	// Print the cart
	printCart(&cart);
	// ** END TEST NUMBER 2 : ShoppingCart --> PASSED **
	// Free the cart memory
	//freeCart(&cart);

	// ** TEST NUMBER 3 : Shopping cart --> PASSED **
	ShoppingCart cart1;
	initializeCart(&cart1);

	// Create a new product
	Product p;
	newProduct(&p);


	// Create a ShoppingItem for the product
	ShoppingItem item = { p, 2 }; // Adding 2 units of the product
	Product a;
	newProduct(&a);
	ShoppingItem item2 = { a, 2 }; // Adding 2 units of the product

	// Add the item to the cart
	addItemToCart(&cart1, &item);
	addItemToCart(&cart1, &item2);

	// Print the cart
	printCart(&cart1);

	// Free the cart memory
	//freeCart(&cart);
	// ** END TEST NUMBER 3 : ShoppingCart --> PASSED **


	return 0;
	*/
	return 0;
}

